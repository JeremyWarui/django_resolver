version: 2

# Seed commands read the demo password from SEED_DEFAULT_PASSWORD (env) — no
# credentials live in source anymore.
paths-ignore:
  # Test files use throwaway credentials (testpass, jwtpass, etc.) — not real secrets
  - "tests/**"
  - "**/tests/**"
  - "conftest.py"
